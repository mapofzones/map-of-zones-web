export enum StatCharts {
  txs = 'txs',
}

export enum SwitchedCharts {
  transfers = 'transfers',
  cashflow = 'cashflow',
}

export enum TokenCharts {
  price = 'price',
  volume = 'volume',
}

export enum TotalCharts {
  tradingVolume = 'tradingVolume',
}
